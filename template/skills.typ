#let lang_skills = (
  "Python", "C", "Java"
)

#let lib_skills = (
  "Litestar", "FastAPI", "Django (DRF)", "Faststream", "dishka"
)

#let infra_skills = (
  "Docker (+Compose)", "Nginx", "Grafana", "NATS", "RabbitMQ", "Minio", "Postgres"
)

#let tool_skills = (
  "pytest", "ruff", "mypy",
)

#let other_skills = (
  "React", "Java Swing", "PySide"
)
